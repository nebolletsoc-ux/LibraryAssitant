Feature, Priority, Description
Availability accuracy regression, RESOLVED, Root cause was configuration, not matching logic: Berkeley Libby was disabled and a stale test library config was left enabled, so Berkeley results (including The Lager Queen of Minnesota) were never checked. Berkeley is now enabled and a full re-scan confirms it as available. Underlying matching code was not changed; add regression coverage (see Technical debt) so a disabled/misconfigured library can't silently zero out results again.
Upload TBR list from CSV, RESOLVED, Implemented. TBR view now has an "Upload TBR list" button with Goodreads/StoryGraph export instructions and an "Upload CSV" control that imports to-read rows into the persisted TBR list, skipping duplicates by ISBN.
Add and search a single title by title/author, RESOLVED, Implemented via the TBR search box, which queries Open Library by free-text title/author and lets you add any result to your list.
Sort button, nice to have, a drop down button with the following choices (Available, Latest Added, Earliest Added, Author: A-Z, Author Z-A, Title A-Z, Title Z-A) which sorts the results accordingly
Informative status bar, RESOLVED, Status bar gives granular live info: whole-list availability scans show the titles currently being checked in flight ("Checking … · N of M books", up to 3 titles + overflow), imports show a live elapsed timer, and scan progress polls until done. Shipped 07 Sep 2026 (start_check_all job carries a "current" list; scan-progress reports it; templates/tbr.html renders it). Import per-title progress still timer-only; a cancel button for the active scan is still open (see tech debt).
Live link to StoryGraph/goodreads list, low priority, web page updates automatically when storygraph/goodreads list is updated
Notifications of books availability, RESOLVED, Implemented. When a refresh detects a previously-unavailable book that is now available, every owner with an email on file and notify_on_available enabled gets an instant alert (mailer.py, stdlib smtplib only). Sending is opt-in per environment: set SMTP_HOST (plus SMTP_PORT/SMTP_USER/SMTP_PASSWORD/SMTP_FROM); without SMTP_HOST all sends are no-ops and the Settings sheet shows an "email not configured" hint. First-scan and available-to-unavailable transitions never alert.
Weekly availability notification, RESOLVED, Implemented. Settings sheet adds a "Weekly availability digest" toggle; the weekly job (send_digest.py CLI — point a Render Cron Job / launchd at it) emails each subscriber a summary of which of their books are currently available, filtered to their enabled libraries. Includes the in-app digest runner (_run_weekly_digest) and full test coverage. Note: actual weekly scheduling is left to your cron, not the web tier, to avoid duplicate sends with multi-worker deploys.
User customization, RESOLVED, Users can now add any Bibliocommons or OverDrive library as a custom entry (POST /api/libraries accepts a label plus overdrive/bibliocommons key) in addition to the shipped LAPL preset; "Add library" view lists which presets are available and lets you add them with one tap.
Faster loading, need to have, initial loading of web site needs to be faster -- comparable to normal web sites
Concise listing, RESOLVED, TBR view is now a single-line-per-book list with color-coded availability dot, no ISBN shown, and per-format availability icons.
UX improvements, low, make it look more professional
Synopsis, RESOLVED, Detail modal shows a synopsis. Books that never had one fetched (everything added via TBR online search or CSV import) now resolve it lazily: opening the detail sheet hits a new POST /api/books/<user_book_id>/synopsis which fetches via Open Library (with a new title/author search fallback for synthetic ISBNs) and caches it on the Book record. Sheet shows "Fetching synopsis…" until it arrives; "No synopsis available for this title." if neither Open Library nor Google Books has one (with GOOGLE_BOOKS_API_KEY set, a Google fallback runs for titles Open Library lacks). See the 09 Sep 2026 pass: work-key lookup fixed + the entire live list backfilled to 138/138.
add ability to search free ebook/audiobook repositories that DO respect IP (gutenberg, openlibrary, etc), low,

Redundant "Hoopla · hoopla" label, RESOLVED, Hoopla availability rows in the detail modal now show the provider once ("Hoopla") instead of "Hoopla · Hoopla". (Also deduped below: most Hoopla entries were duplicates produced by Oakland and Berkeley both tagging the shared Hoopla catalog as library="hoopla".)
Borrow links sometimes wrong, open bug (carried over from docs/buglist item 15), Some "Borrow" links take the user to the wrong book or wrong format. Needs investigation of the Bibliocommons/OverDrive/Hoopla URL-matching logic in library/oakland.py.
Free ebook / shadow-library sites (e.g. gutenberg, openlibrary), no yet implemented, Anna's Archive distributes copyrighted books without publisher/author authorization; integrating it would facilitate copyright infringement, but other web sources do not.
Responsive layout for mobile / iPad / desktop, low, Make the UI lay out well across device classes: single-column, thumb-friendly list on phones; comfortable two-column band on tablets; full-width dense layout on desktop. A lot is already responsive (mobile viewport verified in headless Chrome). Remaining polish: safe-area insets, ≥44px tap targets, sticky-header reflow per breakpoint, keyboard-accessible forms.

--- Technical debt (added after TBR rework, September 2026) ---
No automated test suite, RESOLVED, A real pytest suite now exists and is maintained (84 tests, all offline/mocked) covering books/libraries/import-csv/check-all/clear/synopsis endpoints and the list + book-detail filter logic. Remaining coverage gaps folded into the new "Test coverage gaps" tech-debt item below.
Availability scan concurrency, PARTIAL, check-all now runs concurrently (ThreadPoolExecutor, 8 workers) so a ~150-book scan completes in the time a single library sweep used to take (was 500+ s, now quick), and reports per-title in-flight progress. Remaining: a cancel button that stops the active scan — ties to "Informative status bar" above.
Duplicate Book rows for the same title, medium, Different search results/editions (e.g. "The Lager Queen of Minnesota" vs "The Lager Queen of Minnesota: A Novel") can create separate Book rows with separate synthetic ISBNs, so the same book can appear twice in the TBR list. Needs title normalization or a merge/dedupe step before creating a new Book.
Single hardcoded user (user_id=1), medium, Every model and endpoint assumes one user. Any real multi-user deployment needs an auth layer and per-user scoping instead of the hardcoded default.
No CSRF protection / minimal auth, medium, State-changing endpoints (POST/PATCH/DELETE) are only gated by an optional shared APP_PASSWORD env var with no CSRF protection. Fine for personal/local use; needs hardening before any public multi-user deployment.
Open Library search has no caching or backoff, medium, Rapid repeated searches during testing produced real 503s from Open Library. The app now surfaces a clear "temporarily unavailable" message, but there is no client-side rate limiting, retry/backoff, or short-lived caching of recent queries.
Redundant "Hoopla · hoopla" label, RESOLVED, In the TBR detail modal, Hoopla availability rows showed "Hoopla · hoopla" since library and provider are both effectively "hoopla". providerLabel() now returns a single "Hoopla" for Hoopla rows (verified in headless Chrome). Related: Hoopla rows produced duplicates because Oakland and Berkeley both tag the shared Hoopla catalog as library="hoopla" — now deduped to one row per format in the detail modal (see new dedupe item below).
Legacy CSV-scan app still present alongside TBR view, low, templates/index.html and the "old code/" folder are still in the repo alongside the newer TBR view (templates/tbr.html). Decide whether to retire the legacy view or keep it as a fallback, to reduce duplicate UI/maintenance surface.
No pagination/virtualization for large TBR lists, low, Current lists of 150+ books render fine, but there is no pagination or virtualization if a user's list grows much larger.

--- Revision 04 Sep 2026 (MyNextRead detail-sheet + import feedback pass) ---
Import flow: success popup removed entirely, RESOLVED, Import is now a single button that opens the native file picker; no bottom sheet, no Goodreads/StoryGraph steps, no drag-and-drop. On completion the app returns to the list and shows progress.
Import/availability status bar, RESOLVED, New "status-bar" strip under the header shows live elapsed time during "Importing reading list…" and "Scanning X books…" (now also the in-flight titles being checked) and clears when the scan finishes. Per-title progress during the whole-list scan shipped 07 Sep 2026. "X of Y books checked" counter added to the list footer legend so it is obvious how much of the list has been scanned.
Pending / "Not checked" state, RESOLVED, Books with no availability rows are no longer hidden. They render with a gray tint and a "Not checked" legend swatch, sorting last under the Availability sort. This was the root cause of "import said Done but no books appeared" (imported books have no availability until scanned) and of books vanishing during a scan.
Online search re-added, RESOLVED, The search box returns and is a dedicated ADD flow: a debounced (400 ms, min 3 chars) Open Library search via POST /api/books/search shows an "Add from online search" panel (top 5 results, each with a "+ Add" button or "On your list" if already present); adding stores the book and runs a single availability check. Per the 09 Sep 2026 pass the box no longer filters the local list — searching is strictly for adding, so the local list and the results panel never collide.
Book detail: duplicate rows deduped, RESOLVED, Detail borrow options now collapse to one row per format+library, preferring the best status (available > waitlist > unavailable) when duplicates exist. This fixes the "too many listings" problem, most of which were duplicate Hoopla rows (Oakland + Berkeley both tag the shared Hoopla catalog as library="hoopla").
Join-waitlist reimplemented, RESOLVED, Waitlisted borrow options no longer show a passive "Waitlist" label; they now render a "Join waitlist" button that opens the library's hold/listing URL (row.url) in a new tab, matching the capability of the older version. Note: there is no backend hold API; it deep-links to the library's own hold page.
Maintainer note, MEDIUM, This revision touched templates/tbr.html, app.py (fetch_synopsis title-search fallback + POST /api/books/<id>/synopsis), tests/test_synopsis.py and tests/test_filter_logic.py (detail-sheet mirror), and this wish list. pytest suite is at 81 passing (all offline/mocked). The running :5001 server must be restarted after template changes (Flask debug mode is off; templates are cached).

--- Revision 04 Sep 2026 (part 2: availability scan performance + LAPL default + libraries indicator + clear list) ---
Availability scan performance, RESOLVED, "Refresh availability" was taking 500+ seconds for ~150 books on the live database (fully sequential network checks). check_all_availability now fans out one worker per book through a module-level ThreadPoolExecutor (max_workers=8); each worker pushes its own app context and commits independently, and sqlite connect timeout raised to 30s to absorb write contention. Live run against LAPL (154 books, real network) completed with 100% checked and zero errors in the log. NOTE: 13 titles in the live CSV are pre-existing duplicates (identical ISBN, two user_book rows each) — pre-dates this change; preserved as-is.
LAPL default library, RESOLVED, The shipped preset list is now LAPL-only ("Los Angeles Public Library" via lapl.overdrive.com / Libby); startup seeding enables the preset (previously hardcoded per-library defaults disabled everything). Existing live DB migrated: LAPL added + enabled, Oakland/Berkeley/Redwood City/Hoopla disabled (backup: instance/library_assistant.db.bak). "Add library" view now shows "All supported libraries are already added." since LAPL is seeded; other libraries can still be added as custom OverDrive/Bibliocommons entries.
Libraries indicator, RESOLVED, The Libraries button in the footer now carries a small color dot: red until at least one library is enabled, green once any library is on. updateLibraryIndicator() is called from loadLibraries() and after every toggle, and the badge reflects live enabled state (verified in headless Chrome: toggle off -> red, toggle on -> green).
Clear entire TBR list, RESOLVED, A muted, underlined "Clear entire reading list" control sits at the bottom of the Libraries sheet (deliberately non-prominent). It requires a confirm dialog and calls DELETE /api/books/clear with {"confirm":"clear_all"}; the endpoint only deletes user_book rows (Book/Availability records retained for re-add later) and returns {"deleted": N}. Guarded by a confirmation key to block accidental wipes.
Tests updated, RESOLVED, Presets change broke tests that expected Oakland/Berkeley/Redwood/Hoopla defaults: tests/test_libraries_api.py now asserts the LAPL-only default set plus an empty available list when LAPL is configured, and tests/test_availability.py's default library switcher uses "lapl". New coverage: DELETE /api/books/clear (requires confirmation, deletes entries, retains Book rows, empty-list case). Suite at 84 passing (all offline/mocked).
Maintainer note (part 2), MEDIUM, This revision touched app.py (LIBRARY_PRESETS = LAPL-only via overdrive, preset-driven startup seed with enabled=True, SQLALCHEMY_ENGINE_OPTIONS timeout=30, check_all_availability parallelized via _refresh_one_book, DELETE /api/books/clear), templates/tbr.html (libraries-btn color + badge, updateLibraryIndicator, clear-tbr UI), tests/test_libraries_api.py, tests/test_availability.py, and this wish list. Committed as <NEXT-COMMIT-MESSAGE> and pushed. Verification: pytest 84 pass, node --check JSS OK, headless-Chrome pass on indicator states, add-view empty state, and clear-all (toast + list emptied, then DB restored from /tmp snapshot). Server restarted after changes. The per-book status-bar progress text still awaits the background/async work tracked under "Sequential availability scan" (now RESOLVED as a fast scan; progress reporting is the remaining piece).

--- Revision 05 Sep 2026 (filter bar formatting) ---
Format segmented control, RESOLVED, The three text format pills (All / eBook / Audio) are now a compact icon-only iOS-style segmented control: BookOpen (eBook) · Layers (All) · Headphones (Audio), no labels, no extra width. Gray track (#E5E7EB); the active segment is a white pill with a soft shadow; inactive icons are gray. Order eBook / All / Audio, initial active = All. Verified live on a mobile viewport: click filters the list (eBook 55, All 148), active pill + shadow follows, no JS errors.
Available filter icon, RESOLVED, The dot + "Available" label pill is replaced by a single ListFilter icon (three descending lines). Inactive: gray icon (1.75px) with the green count badge alongside. Active: thick (2.75px) green (#16A34A) icon, badge hidden, list shows only available (58 of 148 in the live list). Toggling is smooth; the Clear-filters reset now also clears this button's active state.
Maintainer note, LOW, Touched templates/tbr.html only (seg/.seg-btn/.available-filter CSS replacing .filter-pill/.dot/.available-pill, filter-bar HTML, seg-btn + available-filter JS handlers, clear-filters reset). pytest 95 pass (unchanged logic); JS node --check OK; server restarted; verified live.
--- Revision 05 Sep 2026 (sort fixes) ---
Sort by author last name, RESOLVED, Author A-Z now sorts by surname ("Kaveh Akbar" -> akbar, "Rebecca Solnit" -> solnit). Also handles already "Last, First" entries and single-name authors without crashing. Verified live on the real list.
Sort by title ignores articles, RESOLVED, Title A-Z drops leading "A / An / The" ("The Apple" sorts under "A"; interior article words like "The Woods" are kept). "Anxious People" is unaffected because the article must be a whole word. Verified live.
Maintainer note, LOW, Touched templates/tbr.html (sortableTitle/sortableAuthor helpers + filteredBooks sort cases) and tests/test_filter_logic.py (mirror + 5 new table-driven sort tests). pytest 90 pass; JS node --check OK; server restarted; verified live. Committed and pushed to phase1-standalone-tbr.

--- Security audit (04 Sep 2026) ---
Scan methods, CLEAN, pip-audit on the installed environment reports zero known CVEs (Flask 3.1.3, Werkzeug 3.1.8, SQLAlchemy 2.0.52, requests 2.34.2, Jinja2 3.1.6, gunicorn 26.2.0). Manual review of app.py + library/ found: ORM-only SQL (no injection); all outbound HTTP has explicit timeouts (4-15s); external hosts are constrained to fixed domains (Open Library/archive.org) or `<key>.overdrive.com` / `<key>.bibliocommons.com` subdomain prefixes so custom library keys cannot be turned into arbitrary SSRF; no eval/exec/subprocess/pickle/render_template_string; debug defaults off (FLASK_DEBUG env only); CSV import is read in-memory, extension-checked, no path traversal/save-to-disk; XSS mitigated (escapeHtml escapes & < > " ' consistently, synopsis and status text set via textContent). No Flask session/cookies, so no SECRET_KEY exposure exists.
Personal DB committed to public repo, RESOLVED, instance/library_assistant.db (your TBR list: titles + ISBNs + add dates) was tracked in git and pushed to the public GitHub repo. Now untracked and excluded by .gitignore (commit 74be596). The DB file remains local-only; future commits will not include it.
No .gitignore, RESOLVED, .gitignore added (commit 74be596) covering .DS_Store, __pycache__/, *.pyc, instance/*.db, instance/*.db.bak, .venv/, .env. All previously tracked junk (.DS_Store, *.pyc, library_assistant.db) was removed from git tracking with `git rm --cached` and is no longer committed.
Unprotected on 0.0.0.0 when APP_PASSWORD is unset, MEDIUM, The app binds host 0.0.0.0 (all interfaces) by default, and the optional Basic-auth gate (APP_PASSWORD) is off unless the env var is set. On a LAN, anyone can then read AND modify the list with no prompt — including DELETE /api/books/clear. Because there is also no CSRF protection, a malicious web page visited on that network could trigger state-changing API calls from a logged-in browser session. For any non-localhost deployment set APP_PASSWORD; adding a CSRF token or requiring auth on state-changing endpoints is the standing item below ("No CSRF protection / minimal auth"). Personal/local use: low real risk.
No dependency manifest / SBOM, RESOLVED, requirements.txt actually IS committed (Flask, Flask-SQLAlchemy, requests, gunicorn; now also psycopg2-binary for the Postgres path). The earlier audit note missed it. The remaining gap is a committed lockfile (versions float on >= floors) and re-running pip-audit on changes.
Basic-auth password compare not constant-time, LOW, require_password compares with plain != (early-exit timing). Practically irrelevant at this scale; swap to hmac.compare_digest if the app ever faces real unauthorized access attempts.
Test coverage gaps, MEDIUM, Array tests cover import/check-all/clear/synopsis/search on the modern TBR path, but the legacy night-mode analyze/quota/job path (analyze_book, jobs dict, /status, /api/books/import from the earlier version) has no automated coverage, and the frontend filter/sort/detail logic is only mirrored in tests, not exercised through a real browser (headless-Chrome checks are ad hoc scripts in /tmp, not a committed suite).

--- Revision 04 Sep 2026 (persistent storage on Render) ---
Deployed data wiped on every redeploy, RESOLVED, The Render free tier uses an EPHEMERAL filesystem, so instance/library_assistant.db died on the next deploy/restart (confirmed live: /api/books went from 143 books + 3 libraries to [] right after a redeploy; the deployed instance's list is unrecoverable). The app now reads DATABASE_URL (normalized to postgresql+psycopg2; bare postgres:// accepted) and falls back to the instance SQLite file when unset, so pointing the deploy at a free hosted Postgres (Neon/Supabase) makes data survive deploys and restarts. New tables auto-create on boot via db.create_all (both engines). psycopg2-binary pinned in requirements.txt. sqlite path and local DB (148 books) verified unchanged; 5 new unit tests for _normalize_database_url. To finish: create the hosted DB and set Render DATABASE_URL (user action), then confirm a book survives a redeploy.
Maintainer note, MEDIUM, Touched app.py (DATABASE_URL support + _normalize_database_url), requirements.txt (psycopg2-binary), tests/test_database_url.py (5 tests), wish list. pytest 95 pass; server restarted locally. Rendered Procfile/gunicorn unchanged (threads 8; Postgres pool_recycle 300, pool defaults OK at this scale).
--- Revision 04 Sep 2026 (async availability scan — mobile fix) ---
Refresh availability failed on mobile, RESOLVED, On the Render deployment, POST /api/books/check-all BLOCKED the HTTP request until all books were scraped — over 2 minutes on the deployed DB (143 books, Oakland + Berkeley + Hoopla enabled), which mobile Safari aborts (~60s) and even desktop tolerates only quietly. Diagnosed live: healthz/tbr/books/libraries/search all returned 200 fast, but check-all exceeded the probe's 120s. The scan is now ASYNC: POST /api/books/check-all returns 202 immediately with a job_id (or 400 if no libraries are enabled), the scan runs in the background on the shared 8-worker pool, and GET /api/books/scan-progress/<job_id> reports {id, total, processed, checked, failures, done}. The frontend polls every 2s and the status bar shows live progress ("Scanning 43 of 148 books · 36s") with an elapsed timer; on completion the button resets, the bar hides, a toast reports "NNN books checked" (or the failure count), and the list reloads. Tapping Refresh while a scan is already running resumes watching the in-flight job (already_running) instead of double-scanning. Scans are serialized server-side (one active job at a time). Verified end-to-end in headless Chrome at a mobile viewport (390x844): 148 books render, refresh shows ticking progress to 148/148, then bar hides / button re-enables / list reloads, no JS exceptions; live checks completed 148/148 with 0 failures on the real network.
Status-bar progress flicker fixed, RESOLVED, scanStatus() previously captured its text in a per-call closure while the 1s ticker kept re-rendering the ORIGINAL message, so live "Scanning N of M" updates would flash back to a static "Scanning availability" every second. The ticker now reads a shared scanStatusText, so progress displays steadily.
Overlapping refresh clicks, RESOLVED, Each Refresh click owned its own poll timer locally, so a second click could not stop the first loop — orphaned pollers kept hitting a finished job forever (observed as endless scan-progress requests in the server log across headless tabs). The poll timer is now module-scoped (scanPollTimer, single active loop; a new click clears the old one) and every exit path resets the button/label/bar.
Maintainer note, MEDIUM, Touched app.py (async check-all: uuid job ids, _active_scan/_scan_lock, _run_scan supervisor thread, /api/books/scan-progress endpoint; checked now means successful scans while processed counts progress), tests/test_availability.py (start_and_wait_scan helper; the two check-all tests adapted to the async contract), templates/tbr.html (poll-based checkAllAvailability, shared scanPollTimer, scanStatusText fix). pytest 90 pass; JS node --check OK; server restarted; verified live + headless-Chrome mobile viewport. Render redeploys on the next push.
Add a library dead end fixed, RESOLVED, "Add a library" previously showed only LAPL as a preset (and LAPL was already configured), so the screen was a dead end. Now the full preset set (LAPL + Oakland, Berkeley, Redwood City, Hoopla, SFPL, SSFPL, Alameda County, Contra Costa County) is restored as OPT-IN offerings: only LAPL is auto-added/enabled on a fresh DB; every other preset appears in the Add list until the user adds it. The available-list sub-labels are now accurate per catalog type (Libby / OverDrive vs Bibliocommons vs Unlimited borrows — previously every non-Hoopla preset wrongly said "via Libby").
Custom library form, RESOLVED, The Add view now includes a form to add any library not in the preset list: library key + display name + optional Libby/OverDrive key and/or Bibliocommons key. Keys are optional — a label-only library is stored, shows as "No online catalog key", and is simply skipped by availability searches (verified live; test-added rows removed afterwards). POSTs to /api/libraries custom-add path; added enabled.
Empty default library state, RESOLVED, Fresh installs now start with NO libraries configured (previously LAPL was auto-added+enabled). All 9 presets are offered in the Add-library screen; the user picks + enables libraries themselves. The existing live DB keeps whatever libraries the user has configured (currently all toggled off).
Maintainer note (part 3), MEDIUM, This revision touched app.py (LIBRARY_PRESETS restored to the 9-library set, startup no longer seeds any library, custom add accepts label-only libraries, available-queue sub labels), templates/tbr.html (add-view presets + custom form + optional-key flow + accurate per-config sub labels), tests/test_libraries_api.py (multi-preset defaults, preset-add, available-only-unconfigured, label-only add succeeds), and this wish list. pytest 85 pass; JS node --check OK; server restarted; verified live. Committed and pushed to phase1-standalone-tbr.

--- Revision 05 Sep 2026 (direction: multi-user, deployable app) ---
Multi-user accounts with per-user TBR lists, RESOLVED, Full auth + per-user scoping shipped: User model (username unique, werkzeug password hash), register/login/logout/me endpoints with Flask session cookies, every /api data route now scopes to the authenticated user (replaced all hardcoded user_id=1 sites), and all mutating /api routes enforce a double-submit-cookie CSRF check (X-CSRF-Token header echoed by the frontend fetch wrapper). Legacy pre-account rows (user_id=1) are adopted by the FIRST registered account; on a fresh DB the first user gets id=1 so the rows are already theirs; if the sequence advanced past 1 the sentinel rows are moved to them (verified via dedicated tests for both paths; later accounts never re-adopt). Frontend: login/register screen (shown when /api/auth/me returns 401), logout button, fetch wrapper injects the CSRF token, bootstrap gated on session. Session signing key: SECRET_KEY env var, or a random per-deployment secret persisted in the app_settings table so sessions survive restarts on Render's ephemeral filesystem. APP_PASSWORD basic-auth gate still available but now redundant (accounts supersede it).
Deploy-readiness checklist, PARTIAL, Durable storage DONE (hosted Postgres via DATABASE_URL, verified live — 148 books / 3 libraries survive redeploys; /healthz reports postgres). no-cache headers DONE (c88ebbb). Auth + per-user scoping + CSRF DONE (multi-user item). Remaining before sharing with anyone else: set SMTP_* env vars to actually send the now-implemented notifications; legacy path test coverage and a lockfile are quality gaps, not blockers.
Maintainer note, MEDIUM, This revision is a plan only: added the two lines above to the wish list; no code changed. pytest remains 95 passing; deploy healthy on Postgres.

--- Revision 05 Sep 2026 (Oakland library was silently returning zero results) ---
Oakland catalog never produced availability, RESOLVED, search_bibliocommons / search_oakland parsed only Hoopla links out of an Oakland search page (_extract_oakland_hoopla), so records tagged library="oakland" could NEVER be created — a full scan with Oakland toggled alone wrote 0 rows. Introduced _extract_bibliocommons_results: splits the page into cp-search-result-item records, and within each record splits per-manifestation blocks (Book / eBook / Audiobook / Music / ...) with real per-manifestation URLs (/v2/record/...), format from display-info-primary + icon-svg class, availability from format-call-wrap available/unavailable plus text refinements ("Check out now on Hoopla" -> available; "All copies in use Holds: 11 on 5 copies" -> unavailable with holds count), and _extract_holds/_extract_wait for hold queues. Catalog rows are tagged with the requested library key once (provider "Catalog"); Hoopla links still come through _extract_oakland_hoopla as library="hoopla". search_oakland now delegates to search_bibliocommons("oaklandlibrary","oakland",...). Verified live: /api/books/5/check on the real TBR list returned 11 rows (9 Oakland manifestations incl. eBooks/Audiobooks + 2 Hoopla). 6 new offline tests in tests/test_bibliocommons.py (ebook-unavailable-with-holds, book-available, hoopla-instant, skip-unrelated, catalog+hoopla combine, search_oakland). pytest 101 pass.
Duplicate titles merged out of the list, RESOLVED, The live list had 12 titles appearing twice — two editions of the same work (one with a real ISBN, one synthetic-*). Merged via /tmp/merge_dupes.py on both the local DB (148->136 rows, backup /tmp/la_before_enum) and the deployed Postgres (145->133, zero duplicate titles): keeper picks real-ISBN + most-availability editions, folds availability rows, deletes the duplicate user_book + orphan Book. Recurrence prevented in add_book + import_tbr_csv via _find_existing_book (ISBN match first, then case-insensitive title+author reuse).
/api/books was slow (N+1), RESOLVED, list_books fetched availability for every book row-by-row (~17s warm on the deployed 148-book list). Now uses SQLAlchemy joinedload eager loading (UserBook.book + Book.availability) and filters cached availability to currently-enabled library keys server-side. Measured live: 17s -> 0.59s.
Whole-list scan prunes disabled-library rows, RESOLVED, start_check_all now deletes cached availability for libraries that are no longer enabled before scanning, so toggling a library off stops its stale "available" outcomes from lingering on the list.
Postgres could not import the TBR list, RESOLVED, models.py Book.isbn was String(20) but synthetic ISBNs are 21 chars (synthetic-<12 hex>), so CSV import / mirroring hit psycopg2 "value too long for type character varying(20)" on Postgres while SQLite silently accepted it. isbn widened to String(64); live column ALTERed. Commit 8c0d715.
Catalog matching refined, RESOLVED, Two follow-up fixes found while validating the parser against the real list: (1) Bibliocommons renders the SHORT main title as item title ('The Wager') with subtitles only in the record text, so a Libby-style item-title gate skipped valid matches -- matching now uses the full item text. (2) Oakland's smart search returns 'Nothing found' for full titles carrying parenthetical series info/sub-series numbers or long subtitles after a colon (confirmed: 'Monster of God' alone returns unrelated monster/god titles, meaning those books genuinely aren't in Oakland's catalog), so search_bibliocommons falls back through cleaned query variants (series/parens stripped, main title up to first colon, author head appended) while still matching strictly against the full original title/author. Result on the live 133-book list after a full scan: 112 books matched (vs 94 with the first draft), 296 availability rows (238 available), 0 duplicate titles, /api/books ~0.55s. Most of the remaining 21 no-match titles (Beverley Nichols gardenesses, obscure indie/Swedish titles) are genuinely absent from Oakland.
Maintainer note, MEDIUM, Touched library/oakland.py (Bibliocommons parser + search_oakland delegation), tests/test_bibliocommons.py (8 tests), wish list. pytest 104 pass. NOTE: because scan now prunes disabled libraries, the previously cached berkeley/hoopla/lapl availability on the deployed Postgres is gone (a fresh scan under those toggles repopulates it). Repeated reminder: the Neon connection string containing its password was pasted into chat — rotate the password (and swap the DATABASE_URL env var on Render) for anything beyond private use.

--- Revision 05 Sep 2026 (default sort) ---
Make "Title A–Z" the default sort, RESOLVED, templates/tbr.html `let sortKey = "title-asc"` (was "status"), clear-filters reset also title-asc; tests/test_filter_logic.py mirror default updated + test_sort_defaults_to_title_asc. pytest 105 pass. Commit 55587f0.
Large maintainer note, MEDIUM, Touched models.py (User model), app.py (auth endpoints, before_request auth+CSRF guards, per-user scoping replaced all user_id=1 sites, DB-persisted SECRET_KEY via app_settings table, legacy-row adoption), templates/tbr.html (auth screen overlay, logout button, fetch wrapper minting X-CSRF-Token, bootstrap gated on session), tests/conftest.py (AuthedClient test client, tester-account autouse fixture, no_auth marker), tests/test_auth.py (14 tests: register/login/logout/me, validation, 401s, isolation, adoption id-1 and id!=1, CSRF block+pass), tests/test_libraries_api.py (scoped to current_user), tests/test_availability.py (failed-check test made deterministic vs worker-pool race), pytest.ini (no_auth marker). pytest 119 pass; JS node --check OK; curl-verified live: register public, me 200, mutating without X-CSRF-Token 403, with header 200, per-user isolation visible (bob/carol see only their own lists). Local server restarted. Render redeploys on the next push; after deploy, register the FIRST account on the live URL to adopt the existing 133-book list+oakland config.

--- Revision 07 Sep 2026 (public-repo security purge) ---
Repo is public, so all history was scrubbed via `git filter-branch` + `gc --prune=now` and force-pushed (main + phase1-standalone-tbr). Removed from ALL history: the dev bundle (LibraryAssistant.zip, contained a copied .venv + old code), the personal to-read exports (books.csv, goodreads_library_export.csv, storygraph_*.csv, test_upload.csv, uploads/*.csv), and the tracked TBR database (instance/library_assistant.db). Pre-rewrite copies of every removed file were recovered to the temp backup (recoverable from the pre-rewrite mirror clone). .gitignore now locks those same paths out of future commits. Audit note: NO credentials were ever in the repo — a per-blob scan of the entire object DB (255 blobs, incl. dangling objects) for gmail addresses, Gmail app-password format, and smtp password literals found nothing; the gmail SMTP creds lived only in chat/env/crontab, not git. Stands: rotate the gmail app password, the Neon password, and the Resend key (all appeared in chat) — update Render env + the Mac crontab (end of DIGEST schedule) if rotated. GitHub may still serve old commit content from cache; a delete-and-recreate would be the only absolute purge. Feature work in this revision: status bar now shows in-flight titles during whole-list scans (scan-progress gains a "current" list; app.py + templates/tbr.html), plus mailer backend-selection/EMAIL_FROM tests and that scan-progress test. pytest 152 pass; JS node --check OK.

--- Revision 07 Sep 2026 (public release roadmap) ---
Release-readiness checklist, PARTIAL, All must-do steps are now RESOLVED (steps 2, 3, 4, 5, 6) or PARTIAL (7, 8); only the operator-owned "nice to have/low" items remain (9, 10, 12) — see the implementation round below. This section IS the release plan.
Step 1 · Rotate every exposed secret, must-do, The Gmail app password, Neon DB password, and Resend key all appeared in chat. Before going public: rotate all three, then update the Render env (DATABASE_URL, RESEND_API_KEY, EMAIL_FROM, SECRET_KEY, APP_PASSWORD) and the Mac crontab (end of DIGEST schedule). NOTE: changing SECRET_KEY signs all sessions out (users simply log back in); the DB-persisted secret means restarting won't, but a manual rotation will.
Step 2 · Enable live email from the web tier, RESOLVED, mailer.py already sends via Resend when RESEND_API_KEY is set (SMTP fallback otherwise) — but today only the Mac crontab's DIGEST job actually emails because it is the only thing holding a key. Set RESEND_API_KEY + EMAIL_FROM (one of the freedns.afraid.org verified sender domains, see cron.example) in the Render web env so notify-on-available, send-test, and the web-triggered digest deliver from the hosted app.
Step 3 · Move the weekly digest off the Mac, RESOLVED, The Mac crontab only fires while the laptop is awake; a public service needs the schedule in the cloud. Add a Render Cron Job that runs send_digest.py against the hosted Postgres (pattern in cron.example), or a scheduled profile/worker; keep the in-app _run_weekly_digest trigger as the manual path.
Step 4 · Terms / privacy / DMCA pages, RESOLVED, Add /terms and /privacy (static HTML, no DB): what's stored (username, password hash, email only if notify enabled, the user's TBR list), single-session-cookie only, no selling/third-party sharing, library data belongs to the catalog owners (the app links out and hosts no copyrighted content; include an abuse/removal contact), and polite scraping (bounded concurrency, no bulk). Link both from the auth screen and footer.
Step 5 · Rate-limit auth endpoints, RESOLVED, Public signups attract bots. Throttle /api/auth/register and /api/auth/login per IP (e.g. 8 req/min -> 429 with Retry-After), lock an account after N failed password attempts (e.g. 10 in 15 min), and add a honeypot field to the register form. Existing CSRF + per-user scoping stay as-is.
Step 6 · Session + response security headers, RESOLVED, The 04 Sep audit covers injection/SSRF/etc. but not headers. On HTTPS-only Render set: SESSION_COOKIE_SECURE=True, HttpOnly + SameSite=Lax session cookie, and an after_request hook adding X-Content-Type-Options: nosniff, Referrer-Policy: same-origin, and a conservative Content-Security-Policy (default-src 'self'; script-src 'self' 'unsafe-inline' only for the inline bootstrap block).
Step 7 · Protect the legacy-row adoption before signups open, PARTIAL, The FIRST registered account adopts any user_id=1 sentinel rows. The live DB currently holds your 133-book list + oakland config — opening public signups risks a stranger adopting it. Before announcement: confirm your account already adopted (and then block further adoption), or clear/re-key the sentinel rows and remove auto-adoption from app.py so fresh deployments start empty.
Step 8 · README + LICENSE, PARTIAL, The public repo has neither. Add a README: one-paragraph pitch, feature bullets, local-dev quickstart (venv, pytest, sqlite vs DATABASE_URL), Render deploy steps (build/start commands + env vars), and the library-catalog context note. Pick a license (MIT recommended), add LICENSE, an issue template, and turn on GitHub Issues for feedback.
Step 9 · Custom domain + branding, nice to have, libraryassitant.onrender.com carries the misspelling and reads as a throwaway URL. Point a real domain at the Render service (the freedns.afraid.org sender domain is separate; a new domain is fine), set Render custom-domain + TLS, and reuse it for EMAIL_FROM and the ToS/privacy links for consistent branding.
Step 10 · Observability + backups, PARTIAL, /healthz exists. sentry-sdk is now wired into app.py (auto-init when SENTRY_DSN is set) — remaining (operator): put the DSN in Render env, add an external uptime check (UptimeRobot free), confirm Neon auto-backups / time-travel are ON for the public DB, and skim Render logs weekly for the first month.
Step 11 · Account deletion + data export, RESOLVED, Trust- and privacy-building: add a "Delete my account" action (removes User + its user_book rows) and a "Download my list (CSV)" export that mirrors the existing import. Avoids support burden once strangers sign up.
Step 12 · Soft launch before anyone else, low, Run a closed beta first: hand the link to a handful of friends, watch auth/scan error logs, fix whatever breaks, then open Issues and announce. Do NOT announce while any must-do step above is still open.

--- Revision 07 Sep 2026 (public release implementation round) ---
Rolled the roadmap's code-side work into app.py + templates + tests (all committed and force-pushed to phase1-standalone-tbr on this round):
- Step 2: web tier already mails via Resend; added a startup warning when RESEND_API_KEY is set but EMAIL_FROM is missing (so a deploy can't silently send from onboarding@resend.dev). Remaining (operator): put RESEND_API_KEY + EMAIL_FROM into the Render web-service env.
- Step 3: new POST /api/system/digest webhook — any HTTP-capable scheduler (cron-job.org, GitHub Actions, Render Cron Job curl) can fire the weekly digest with a single shared CRON_TOKEN via Authorization: Bearer or X-Cron-Token (hmac.compare_digest); endpoint is 404 while CRON_TOKEN is unset, 401 on mismatch, and returns {"digest_sent": N}. cron.example gained "Option C: webhook digest". Remaining (operator): generate CRON_TOKEN, add to Render env, create a Render Cron Job (or cron-job.org) that curls it. The Mac crontab continues to push until then.
- Step 4: public /terms and /privacy pages (templates/terms.html, templates/privacy.html), linked from the auth card and the footer; exempted from the shared APP_PASSWORD gate and reachable logged-out.
- Step 5: /api/auth/register + /api/auth/login now throttle per-IP at 8 req/60s -> 429 with Retry-After (in-memory per-process; fine on single-worker); register gained a honeypot field ("website", hidden off-screen in the form, real bots get a fake 201 and no account); an account locks after 10 failed password attempts for 15 min (DB-backed in app_settings key login_fail:{username}, so it survives restarts; 429 + Retry-After while locked, clears on success). Password min length 6 and username <= 80 enforced, 409 on duplicates as before.
- Step 6: ProxyFix (x_for/x_proto=1) so request.is_secure reflects Render's TLS; session cookie is HttpOnly + SameSite=Lax statically and flagged Secure per-request inside the after_request hook (right before Flask persists the cookie, so it can't leak between requests); SESSION_COOKIE_SECURE=0 forces it off behind a plain-HTTP proxy. after_request now adds X-Content-Type-Options: nosniff, Referrer-Policy: same-origin, and a conservative CSP (script-src 'self' 'unsafe-inline').
- Step 7: legacy-row adoption is now OPT-IN. register() only calls _adopt_legacy_user_rows() for the very first user AND when ADOPT_LEGACY_ROWS=1; auto-claim is removed, so a stranded user_id=1 list can never be grabbed by a random signup. Remaining (operator): confirm/claim your own live list before opening signups (see runbook).
- Step 11: GET /api/books/export.csv downloads the list as a Goodreads-shaped CSV (Title, Author, Exclusive Shelf=to-read, ISBN13) that import can re-import; an Export button sits next to Import and "Download my list (CSV)" in Settings. DELETE /api/auth/account (CSRF-protected, requires {"confirm":"delete_account"}) removes the user, their user_books, their library configs, and any Book rows that no other account references (shared books are kept); Settings gained a red "Delete my account" button with a double confirm.
- Other: /api/books/<id>/synopsis is now scoped to the requesting user (404 for another account's row, closing a pre-accounts data leak).
- Tests: new tests/test_release_readiness.py (headers, Secure cookie over https vs plain http, terms/privacy public, register rate-limit 429, login lockout + retry-after + clear, honeypot no-account, second-signup can't claim legacy rows + flag still can't for non-first users, export round-trip, account deletion incl. shared-book preservation + orphan cleanup + confirmation guard + post-delete 401, digest webhook 404/401/200-with-mail, cross-user synopsis 404). conftest clears _rate_hits + login_fail rows between tests. Full suite: 170 passed; JS node --check OK. ReadMe.md rewritten (public-facing) + LICENSE (MIT) added.
Remaining operator steps for release: Step 1 (rotate Gmail/Neon/Resend), Step 2 env (RESEND_API_KEY + EMAIL_FROM + GOOGLE_BOOKS_API_KEY + SECRET_KEY), Step 3 DONE (digest scheduled via GitHub Actions, CRON_TOKEN in Render + repo secret), Step 7 (benc already owns the sentinel rows; nothing to adopt — just don't clear rows 1 before signups), Step 8 (GitHub Issues on + issue templates), Step 9 (custom domain), Step 10 (UptimeRobot/Sentry/Neon backups), Step 12 (closed beta).

--- Revision 09 Sep 2026 (live-app pass: search regression, availability state, synopsis coverage, cloud digest, email verification) ---
Search box add-only regression, RESOLVED, The search box is for ADDING books (online search preview), not filtering: it previously also filtered the local list (renderBooks on every keystroke) and its results rendered below the fold. Now input/clear handlers only refresh the online-results panel, filteredBooks() ignores searchQuery, #online-results renders above the book list, and the placeholder says "Search to add a book…". New tests/test_tbr_search.py (4 tests). Commit edb15af.
Checked-but-empty availability state, RESOLVED, Books that HAD been scanned but turned up nothing showed as grey "Not checked" — indistinguishable from never-scanned books. statusOf now returns "unavailable" when a checked book has zero matching rows, the list shows them under Unavailable, and the detail sheet says "No copies currently available at your libraries." vs "Availability has not been checked yet." New tests/test_tbr_availability_state.py (3 tests). Commit 5ad568e.
Synopsis coverage 10 -> 138, RESOLVED, Only 10/138 live books had a synopsis. Root cause: fetch_synopsis read the work key from OpenLibrary search docs' seed field, which is usually EMPTY (the key lives on doc.key), so lookups silently found nothing; 4s timeouts made intermittent /works slowness fail the step. Fixed the doc-key lookup, raised timeouts to 8s, and added an optional Google Books fallback (intitle/inauthor match, then a relaxed "title + author" query — strict matches often surface blurb-less editions — picking the first described volume; earlier draft aborted on StopIteration when an edition set had no blurbs, fixed). With Google Books API key the whole 138-book list was backfilled to 100% (two genuinely missing blurbs hand-written). Backfill was throttled/parallel (6 workers) against the live Neon DB; go-forward fetches on sheet open use the same code path. New tests in tests/test_synopsis.py; README documents GOOGLE_BOOKS_API_KEY (API-restrict to "Books API"). Commits d2d9bf8, 64cf0b6.
Weekly digest scheduled in the cloud, RESOLVED, The Mac crontab is retired. GitHub Actions (.github/workflows/digest.yml) fires POST /api/system/digest every Monday 09:00 UTC using a CRON_TOKEN repo secret (Render Cron Jobs are paid-plan only); manual runs verified {"digest_sent":1}. Render env keeps the same CRON_TOKEN for the web tier. Step 3 of the release roadmap is DONE.
Email verification via click-link, RESOLVED, New/changed email in Settings now triggers a verification email with a one-click link (token stored only as SHA-256, 72h expiry; GET /verify-email conversationally marks verified). Alerts and the weekly digest only go to verified addresses; Settings shows a ✓ once verified with a resend button (POST /api/user/preferences/resend-verification rotates the token). The live benc address was grandfathered verified on migration; cathy's account has none. New tests/test_email_verification.py (8 tests). Live users table ALTERed (email_verified, email_verify_hash, email_verify_expires). Commit e6d108d.
Maintainer note, MEDIUM, Touched app.py (fetch_synopsis doc-key + 8s timeouts + Google fallback loop, verification helpers + /verify-email page + resend route, verified-gated _run_weekly_digest and _notify_available), models.py (3 new User columns), templates/tbr.html (settings verification note/resend, add-only search, unavailable-state suffix), tests (test_tbr_search, test_tbr_availability_state, test_synopsis, test_email_verification, updated test_notifications + test_release_readiness to mark verified addresses), ReadMe.md (env table), RUNBOOK.md (digest steps, Step 3 env, Step 7 users). pytest 188 pass; JS node --check OK; live Neon migrated + backfilled. Render re-deploys on push to phase1-standalone-tbr.

